directive @authField(requireRole: ENUM_ROLE) on FIELD_DEFINITION

enum ENUM_ROLE {
  ADMIN
  REVIEWER
  USER
}

interface ByUser {
  _id: String!
  fullName: String!
}

scalar SCalar

input BaseInput {
  nodeId: String!
  profileId: String!
}

enum ENUM_USER_TYPE {
  MEMBER
  CLIENT
}
